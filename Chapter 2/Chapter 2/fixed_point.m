function y = fixed_point ( f, x0, TOL, Nmax )%FIXED_POINT   locate the fixed point of an arbitrary function using general%              fixed_point iteration%%     calling sequences:%             y = fixed_point ( 'g', x0, TOL, Nmax )%             fixed_point ( 'g', x0, TOL, Nmax )%%     inputs:%             g       string containing name of m-file defining the%                     iteration function%             x0      initial approximation to location of fixed point%             TOL     absolute error convergence tolerance%             Nmax    maximum number of iterations to be performed%%     output:%             y       approximate value of fixed point%%     NOTE:%             if FIXED_POINT is called with no output arguments, the %             iteration number and the current approximation to the %             fixed point are displayed%%             if the maximum number of iterations is exceeded, a message%             to this effect will be displayed and the most recent%             approximation will be returned in the output value%old = x0;  older = x0;for i = 1 : Nmax    new = feval(f,old);		if ( nargout == 0 )	   disp ( sprintf ( '\t\t %3d \t %.16f \n', i, new ) )	end		if ( i > 2 )	   lambda = ( new - old ) / ( old - older );	   eest = lambda / ( 1 - lambda ) * ( new - old );	   if ( abs(eest) < TOL ) 	      if ( nargout == 1 )	         y = new;	      end	      return	   end	end	older = old;	old = new;	enddisp('Maximum number of iterations exceeded')if ( nargout == 1 ) y = new; end