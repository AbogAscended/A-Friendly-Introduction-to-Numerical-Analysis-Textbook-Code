function y = neville ( xi, fi, t )%NEVILLE      evaluate the Lagrange form of the interpolating polynomial %             associated with a given set of interpolating points and%             function values at a single point%%     calling sequences:%             y = neville ( xi, fi, t )%             neville ( xi, fi, t )%%     inputs:%             xi      vector containing the interpolating points%             fi      vector containing function values%                     the i-th entry in this vector is the function%                     value associated with the i-th entry in the 'xi'%                     vector%             t       value of independent variable at which interpolating%                     polynomial is to be evaluated%%     output:%             y       value of the interpolating polynomial associated with %                     the given set of interpolating points and function%                     values at the indicated value of the indepedent%                     variable%n = length ( xi );m = length ( fi );if ( n ~= m )   disp ( 'neville error: number of ordinates and number of function values must be equal' )   returnendtemp = fi;for j = 2:n    for i = n:-1:j	    temp(i) = ( (t-xi(i-j+1))*temp(i) - (t-xi(i))*temp(i-1) ) / ...		          ( xi(i) - xi(i-j+1) )	endendif ( nargout == 0 )   disp ( temp(n) )else   y = temp(n);end